import Foundation
import os.log

/// @ai[2025-01-18 08:00] ログ出力の統一ラッパークラス
/// 目的: loggerとprintを統一したインターフェイスで提供し、デバッグ時の可視性を向上
/// 背景: macOSのlogger.debug()はデフォルトで表示されず、デバッグが困難
/// 意図: すべてのログ出力をprint()で統一し、デバッグ時の可視性を確保
/// @ai[2025-11-25 18:10] verboseモード対応を追加
/// 目的: 詳細ログを条件付きで出力し、通常実行時の可読性を向上
/// 背景: 冗長なDEBUG出力が通常実行時のログを読みにくくしている
/// 意図: verboseモード時のみ詳細ログを表示し、通常実行時は簡潔な出力に
public class LogWrapper {
    private let logger: Logger
    private let subsystem: String
    private let category: String
    
    /// @ai[2025-11-25 18:10] verboseモードのグローバルフラグ
    /// 目的: アプリケーション全体でverboseモードの状態を共有
    /// 背景: 複数のLogWrapperインスタンス間でverbose状態を共有する必要がある
    /// 意図: グローバルフラグにより、すべてのログ出力で一貫した動作を実現
    /// @ai[2025-11-25 18:15] Swift 6並行処理安全性対応
    /// 目的: 共有可変状態の並行処理安全性エラーを解決
    /// 背景: Swift 6の厳格な並行処理チェックにより、グローバル変数へのアクセスがエラーになる
    /// 意図: nonisolated(unsafe)により、アプリケーション起動時に一度だけ設定されるフラグとして安全に使用
    nonisolated(unsafe) public static var isVerbose: Bool = false
    
    public init(subsystem: String, category: String) {
        self.subsystem = subsystem
        self.category = category
        self.logger = Logger(subsystem: subsystem, category: category)
    }
    
    /// @ai[2025-01-18 08:00] デバッグレベルのログ出力
    /// 目的: デバッグ情報を統一された形式で出力
    /// 背景: logger.debug()は表示されないため、print()を使用
    /// 意図: デバッグ時の可視性を確保し、開発効率を向上
    /// @ai[2025-11-25 18:10] verboseモード時のみ出力するように変更
    /// 目的: 通常実行時は詳細ログを非表示にし、可読性を向上
    /// 背景: 冗長なDEBUG出力が通常実行時のログを読みにくくしている
    /// 意図: verboseモード時のみ詳細ログを表示
    public func debug(_ message: String) {
        guard LogWrapper.isVerbose else {
            logger.debug("\(message)")
            return
        }
        let timestamp = DateFormatter.logTimestamp.string(from: Date())
        print("🔍 [\(timestamp)] [\(category)] \(message)")
        logger.debug("\(message)")
    }
    
    /// @ai[2025-01-18 08:00] 情報レベルのログ出力
    /// 目的: 一般的な情報を統一された形式で出力
    /// 背景: 処理の流れや重要な情報を記録
    /// 意図: 実行フローの可視化と問題の特定を容易にする
    public func info(_ message: String) {
        let timestamp = DateFormatter.logTimestamp.string(from: Date())
        print("ℹ️ [\(timestamp)] [\(category)] \(message)")
        logger.info("\(message)")
    }
    
    /// @ai[2025-01-18 08:00] 警告レベルのログ出力
    /// 目的: 警告情報を統一された形式で出力
    /// 背景: 処理は継続するが注意が必要な状況を記録
    /// 意図: 潜在的な問題を早期に発見し、対処する
    public func warning(_ message: String) {
        let timestamp = DateFormatter.logTimestamp.string(from: Date())
        print("⚠️ [\(timestamp)] [\(category)] \(message)")
        logger.warning("\(message)")
    }
    
    /// @ai[2025-01-18 08:00] エラーレベルのログ出力
    /// 目的: エラー情報を統一された形式で出力
    /// 背景: 処理が失敗した場合の詳細情報を記録
    /// 意図: エラーの原因特定と修正を迅速に行う
    public func error(_ message: String) {
        let timestamp = DateFormatter.logTimestamp.string(from: Date())
        print("❌ [\(timestamp)] [\(category)] \(message)")
        logger.error("\(message)")
    }
    
    /// @ai[2025-01-18 08:00] 成功レベルのログ出力
    /// 目的: 成功情報を統一された形式で出力
    /// 背景: 処理が正常に完了した場合の確認情報を記録
    /// 意図: 処理の成功を明確に示し、デバッグを支援
    public func success(_ message: String) {
        let timestamp = DateFormatter.logTimestamp.string(from: Date())
        print("✅ [\(timestamp)] [\(category)] \(message)")
        logger.info("\(message)")
    }
    
    /// @ai[2025-11-25 18:30] 長いテキストのフォーマット表示
    /// 目的: 長いテキスト（プロンプトなど）を適切にフォーマットして表示
    /// 背景: プロンプト表示の改善ロジックが複数箇所に重複している
    /// 意図: コードの重複を排除し、保守性を向上
    /// @ai[2025-11-25 18:30] verboseモード対応を追加
    /// 目的: verboseモード時のみ詳細を表示し、通常実行時は要約のみ表示
    /// 背景: 冗長なプロンプト出力が通常実行時のログを読みにくくしている
    /// 意図: verboseモード時のみ詳細ログを表示
    public func debugLongText(_ label: String, _ text: String, maxLength: Int = 500) {
        guard LogWrapper.isVerbose else {
            // verboseモード無効時は要約のみ表示
            if text.count > maxLength {
                logger.debug("\(label) (長さ: \(text.count)文字)...以下省略")
            } else {
                logger.debug("\(label) (長さ: \(text.count)文字)")
            }
            return
        }
        
        // verboseモード有効時は詳細を表示
        if text.count > maxLength {
            debug("\(label) (先頭\(maxLength)文字): \(String(text.prefix(maxLength)))...以下省略(全\(text.count)文字)")
        } else {
            debug("\(label): \(text)")
        }
    }
}

/// @ai[2025-01-18 08:00] ログ用の日時フォーマッター
/// 目的: ログ出力時のタイムスタンプを統一された形式で提供
/// 背景: デバッグ時の時系列把握を容易にする
/// 意図: ログの可読性と時系列の追跡を向上
extension DateFormatter {
    static let logTimestamp: DateFormatter = {
        let formatter = DateFormatter()
        formatter.dateFormat = "HH:mm:ss.SSS"
        return formatter
    }()
}
